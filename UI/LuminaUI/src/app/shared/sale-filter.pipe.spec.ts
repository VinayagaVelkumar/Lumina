import { SaleFilterPipe } from './sale-filter.pipe';

describe('SaleFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SaleFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
